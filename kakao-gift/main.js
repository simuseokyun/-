const solution = () => {
    // if (friends.length < 2 || friends.length > 50) {
    //     return;
    // }
    const friends = ['a', 'b', 'c'];
    const gift = ['a b', 'b a', 'c a', 'a c', 'a c', 'c a'];
};
solution();
